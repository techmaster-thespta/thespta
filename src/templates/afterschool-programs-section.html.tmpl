<!-- ===== Afterschool programs ===== -->
<section class="thes__section thes__section--tint">
  <div class="thes__wrap">
    <p class="thes__committees-note">These are run by outside providers, not the PTA — each program has its own registration, pricing, and contact info. See the flyer for full details.</p>
    <div class="thes__program-grid">
{{AFTERSCHOOL_PROGRAM_CARDS}}
    </div>
  </div>
</section>
