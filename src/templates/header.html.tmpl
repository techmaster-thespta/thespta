<header class="thes__header">
  <div class="thes__header-wrap">
    <a class="thes__header-brand" href="{{page_urls.home}}">{{org_name}}</a>
    <nav class="thes__header-nav" aria-label="Primary">
      <a href="{{page_urls.home}}">Home</a>
      <a href="{{page_urls.about}}">About</a>
      <a href="{{page_urls.get_involved}}">Get Involved</a>
      <a href="{{page_urls.events}}">Events</a>
    </nav>
  </div>
</header>
