<!--
  THUNDER HILL PTA — COMMITTEES (generated by build.py — do not hand-edit pages/get-involved/committees.html)
  Committee data lives in config/committees.json; the shared volunteer
  form lives in config/site.json's `volunteerForm` block. Edit those, not
  this file, to add/remove/edit a committee or change the form.
-->
{{TOKENS}}

<div class="thes">

  <div class="thes__pagehead">
    <div class="thes__pagehead-text">
      <span class="thes__eyebrow">Get Involved</span>
      <h1>{{PAGE_TITLE}}</h1>
      <p>Every committee below is a way to help {{school_short_name}} — pick whatever fits your time and interests.</p>
    </div>
    <div class="thes__pagehead-media">
      <img src="{{PAGE_HEADER_IMAGE_URL}}" alt="{{school_name}}">
    </div>
  </div>

{{COMMITTEES_SECTION}}

{{FOOTER}}

</div>
