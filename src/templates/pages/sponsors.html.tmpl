<!--
  THUNDER HILL PTA — BECOME A SPONSOR (generated by build.py — do not
  hand-edit pages/sponsors.html)
  Sponsorship tier/benefit info lives in config/sponsorship.json. The
  list of businesses that have already joined lives separately in
  config/sponsors.json (see .claude/skills/add-sponsor/SKILL.md) and
  reuses the same "Our Sponsors" section the Home page shows — same
  empty-list-means-no-section pattern, so this section quietly doesn't
  appear until the first real sponsor is added.
-->
{{TOKENS}}

<div class="thes">

  <div class="thes__pagehead">
    <div class="thes__pagehead-text">
      <span class="thes__eyebrow">Support Our School</span>
      <h1>{{PAGE_TITLE}}</h1>
      <p>Support {{school_short_name}}, invest in our community, and make a lasting impact.</p>
    </div>
    <div class="thes__pagehead-media">
      <img src="{{PAGE_HEADER_IMAGE_URL}}" alt="{{school_name}}">
    </div>
  </div>

{{SPONSORSHIP_SECTION}}

{{SPONSORS_SECTION}}

{{FOOTER}}

</div>
